namespace MathEditor.Models;

public enum Enums
{
    Idle,
    Pan,
    CreateTextField,
    CreateMathField
}

public enum ResizeDirection
{
    TopLeft,
    Top,
    TopRight,
    Right,
    BottomRight,
    Bottom,
    BottomLeft,
    Left
}